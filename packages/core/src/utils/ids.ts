export const buildWorkflowDataId = (workflowId: string) => {
  return `data-growly-workflow="${workflowId}"`;
};

export const buildStepDataId = (stepId: string) => {
  return `data-growly-step="${stepId}"`;
};
